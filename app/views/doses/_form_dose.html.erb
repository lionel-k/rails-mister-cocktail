<%= simple_form_for [@cocktail, @dose] do |f| %>
<%= f.association :ingredient, collection: Ingredient.all.order(:name), prompt: "Choose an ingredient", input_html: { class: 'select2' } %>

<%= f.input :description %>
<%= f.submit "Add an ingredient", class: "btn btn-primary" %>
<% end %>


